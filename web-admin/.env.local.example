NEXT_PUBLIC_API_BASE_URL=https://api.getpraying.com

# Production-only CDN/asset prefix. Leave unset locally — wrong values cause /_next/static 404s in dev.
# NEXT_ASSET_PREFIX=

# Advanced (Docker): skip dev port collision guard — SKIP_WEB_ADMIN_PORT_CHECK=1
