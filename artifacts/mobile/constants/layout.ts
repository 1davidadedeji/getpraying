/** Shared breakpoints and max widths for phone / tablet / web. */
export const LAYOUT = {
  contentMaxWidth: 760,
  feedMaxWidth: 820,
  authMaxWidth: 480,
  welcomeMaxWidth: 560,
  tabletMinWidth: 768,
} as const;
