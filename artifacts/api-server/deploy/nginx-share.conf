# Production today: share pages + AASA live on api.getpraying.com (Express shareRouter at root).
# share.getpraying.com currently hits a stale app and returns "Cannot GET /post/…".
#
# When you want a dedicated share host, proxy it to the SAME upstream as api.getpraying.com:
#
# server {
#   listen 443 ssl http2;
#   server_name share.getpraying.com;
#
#   client_max_body_size 0;
#
#   location / {
#     proxy_pass http://127.0.0.1:5000;  # same port as api.getpraying.com
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
# }
#
# Then set SHARE_WEB_ORIGIN=https://share.getpraying.com on the API and
# EXPO_PUBLIC_SHARE_WEB_ORIGIN=https://share.getpraying.com in mobile builds,
# and restore associatedDomains / intentFilters to share.getpraying.com.
