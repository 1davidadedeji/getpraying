# Copy to .env in this folder (or use repo root .env — dotenv loads from process cwd).
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME
PORT=3001

# Expo push — project @timelesz_dave/get-praying (NOT the deleted mobile project)
# https://expo.dev/accounts/timelesz_dave/projects/get-praying → Access tokens → Create
EXPO_ACCESS_TOKEN=

# Optional alias (same effect):
# EXPO_PUSH_ACCESS_TOKEN=
